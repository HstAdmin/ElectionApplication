@using Hst.Model
@model IEnumerable<Organisation>
@{
    ViewData["Title"] = "OrganisationList";
}


<h2>OrganisationList</h2>





<p class="text-right">
    @Html.ActionLink("CreateNew","Create", "Organisation", null, null)
</p>
@*<button type="button" class="btn btn-primary" data-toggle="ajax-modal" data-target="#addOrganisation"
    data-url="@Url.Action("Create")">Create</button>*@
@*<p>
        <a asp-action="Create">Create New</a>
    </p>*@
@if (ViewBag.Message != null)
{

    <div class="alert alert-success" role="alert">
        @ViewBag.Message
    </div>
}


<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.O_Name)
            @*Name*@
        </th>
        <th>
            @Html.DisplayNameFor(model => model.O_Address)
            @*Address*@
        </th>
        <th>
            @Html.DisplayNameFor(model => model.O_CityID)
            @*CityID*@
        </th>
        <th>
            @Html.DisplayNameFor(model => model.O_StateID)
            @*StateID*@
        </th>

        <th></th>
    </tr>


    @*@if (Model != null)
            {
        @if (Model != null && Model.organisationList != null && Model.organisationList.Count() > 0)
        {
            //for (int i = 0;i < Model.organisationList.Count; i++)
            //{
            //    var item = Model.organisationList[i];
            foreach(var item in Model.organisationList) { *@
    @if (Model != null)
    {
        @foreach (var item in Model)
        {

            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.O_Name)
                    @*@item.O_Name*@
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.O_Address)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.O_CityID)
                    @*@item.O_CityID*@
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.O_StateID)
                </td>
                <td>
                    @Html.ActionLink("Edit", "Edit", "Organisation", new { id = item.O_ID }) |
                    @*@Html.ActionLink("Delete", "Organisation", new { id = item.O_ID })*@
                    <a asp-action="Delete" asp-route-id="@item.O_ID">Delete</a>
                </td>
            </tr>

        }
    }



    else
    {
        <td colspan="4" class="text-center"><b>No Data Available . Please Add Data By Click On Create Button</b></td>
    }
</table>     